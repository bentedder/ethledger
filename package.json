{
  "name": "etherfam",
  "version": "1.0.0",
  "description": "Note: I added an additional layer of \"why\" to the assignment by way of wrapping it in a use case for such an application.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "heroku-postbuild": "npm run ui",
    "ui": "cd ui && yarn && yarn build && cp -rf build ../build"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "body-parser": "^1.18.2",
    "cors": "^2.8.4",
    "dotenv": "^5.0.1",
    "es6-promise": "^4.2.4",
    "express": "^4.16.3",
    "isomorphic-fetch": "^2.2.1",
    "mongoose": "^5.0.16"
  }
}
